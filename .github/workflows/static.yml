# 🚀 Workflow para desplegar una app Angular en GitHub Pages
name: Deploy Angular App to GitHub Pages

on:
  push:
    branches: ["main"]  # Se ejecuta cuando hay cambios en la rama 'main'
  workflow_dispatch:  # Permite ejecución manual desde GitHub Actions

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: github-pages
    steps:
      - name: Checkout código fuente
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Instalar angular cli
        run: npm install -g @angular/cli

      - name: eliminar node_modules
        run: rm -rf node_modules package-lock.json

      - name: eliminar cache
        run: npm cache clean --force
        
      - name: Instalar dependencias
        run: npm install --legacy-peer-deps

      - name: Construir aplicación Angular
        run: ng build --configuration production --base-href /

      - name: Subir artefacto para GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist  # 📌 Asegúrate de que coincide con el outputPath en angular.json

      - name: Desplegar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
