import { Routes } from '@angular/router';
import { ControlEntregasComponent } from './control-entregas.component';

export default [
    {
        path: '',
        component: ControlEntregasComponent
    }
] as Routes;
